version: "3.9"

services:
  web:
    build: ./web
    ports:
      - "8080:8080"
    volumes:
      - shared_data:/data
    depends_on:
      - worker

  worker:
    build: ./worker
    volumes:
      - shared_data:/data
    deploy:
      replicas: 1

volumes:
  shared_data

version: "3.9"

services:
  web:
    build: ./web
    ports:
      - "8080:8080"
    environment:
      - FLASK_APP=app.py
      - FLASK_DEBUG=1 
    volumes:
      - shared_data:/data
      - ./web:/app
    depends_on:
      - worker
      - redis

  worker:
    build: ./worker
    volumes:
      - shared_data:/data
      - kraken_db:/virology_db
    depends_on:
      - redis
    deploy:
      replicas: 1

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

volumes:
  shared_data:
  kraken_db:
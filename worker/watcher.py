
import json
import time
import os
from shared.models import Job


def main():
    """Loop that watches for new jobs and executes them."""
    jobs_dir = "shared/data"
    while True:
        pass # jasses man
        # time.sleep(3)
        # for job in os.listdir(jobs_dir):
            

if __name__ == "__main__":
    main()
